management:
  endpoints:
    web:
      exposure:
        include: "gateway"
  endpoint:
    gateway:
      enabled: true

spring:
  cloud:
    gateway:
      routes:
        - id: rest-service
          uri: http://localhost:19090
          predicates:
            - Path=/rest/**
